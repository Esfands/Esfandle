import { Paper } from '@mui/material';

const Cell = () => {

    return (
        <Paper sx={{ height: 70, width: 60 }} style={{backgroundColor: "#1b2126", color: "white"}}>A</Paper>
    )
};

export default Cell;